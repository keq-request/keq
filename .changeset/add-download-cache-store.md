---
"@keq-request/cli": minor
---

**Feat:** Add download cache store system with `FileSystemCacheStore`, `MemoryCacheStore`, `NullCacheStore`, and `NamespacedCacheStore` implementations. Supports `--no-cache` flag in `build` command and a new `cache clear` subcommand for managing cached downloads.
