---
"@keq-request/cli": minor
---

**Feat:** Add `keq describe` command with `operation` and `schema` subcommands to display detailed API structure definitions including parameters, request body, and responses.
