---
"@keq-request/cli": minor
---

**Feat:** Add isolated code generation mode that produces standalone operation files with inline schema declarations and per-module `KeqRequest` instances, enabling independent usage without shared schema imports.
