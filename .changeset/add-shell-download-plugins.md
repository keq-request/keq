---
"@keq-request/cli": minor
---

**Feat:** Add `DownloadByBashPlugin` and `DownloadByShPlugin` as built-in plugins, enabling OpenAPI/Swagger spec retrieval by executing script files via `bash://` and `sh://` URL protocols. Ideal for monorepo setups where backend services don't need to be running.
